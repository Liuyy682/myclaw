# Sol + Luna 子代理协作约定

## 角色分工

- Sol 是主代理，负责理解需求、思考方案、拆解任务，以及在执行完成后进行最终审核。
- 默认执行代理是自定义代理 `luna_worker`。

## 委派规则

- 对需要执行、实现、测试或验证的任务，Sol 应自动将边界清晰的执行工作委派给 `luna_worker`，再由 Sol 汇总结果并完成最终审核。
- 委派 `luna_worker` 时选择自定义代理 `luna_worker`，创建新代理时不要继承主线程历史；不要显式传入 `model`，也不要设置 `agents.default_subagent_model`，由自定义代理文件解析模型配置。
- 只读任务也可以委派给 `luna_worker` 做范围明确的调查；只读委派不得修改工作区。
- Luna 返回结果后，Sol 必须检查文件范围、行为边界和验证证据，必要时补充检查，再向用户报告。
